version: '2.4'
services:
  backend:
    entrypoint: sh -c "tail -f /dev/null"

    environment:
      CIS2_IMPORT_VIEW_TOKEN: "testViewTokenCIS2" #betterleaks:allow
      CIS2_IMPORT_TOKEN: "replacethis" #betterleaks:allow
      CIS2_IMPORT_URL: "replacethis"

    volumes:
      - ./backend/:/app/src
